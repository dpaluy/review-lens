# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore all environment files (except templates).
/.env*
!/.env.example
!/.env.*.example

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# Ignore master key and credentials (never commit secrets).
/config/master.key
/config/credentials/*.key

# Track .kamal/secrets because it contains 1Password references and shell wiring,
# not secret values. Ignore local override files only.
/.kamal/secrets.local
/.kamal/secrets.*.local
/.kamal/.env

# Ignore node_modules and compiled assets.
/node_modules/
/app/assets/builds/*
!/app/assets/builds/.keep
/public/assets

# Ignore local AI transcripts scratch (real transcripts under /ai-transcripts must be committed).

# Ignore editor / OS files.
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
*.rbc

# Ignore test/coverage artifacts.
/coverage/
/coverage.data

# Ignore local scrape-probe output and captured fixtures not meant for the repo.
/script/output/

# Ignore key files for decrypting credentials and more.
/config/*.key


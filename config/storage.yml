test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

digitalocean_spaces:
  service: S3
  access_key_id: <%= Rails.application.credentials.dig(:active_storage, :access_key_id) %>
  secret_access_key: <%= Rails.application.credentials.dig(:active_storage, :secret_access_key) %>
  region: <%= Rails.application.credentials.dig(:active_storage, :region) %>
  bucket: <%= Rails.application.credentials.dig(:active_storage, :bucket) %>
  endpoint: <%= Rails.application.credentials.dig(:active_storage, :endpoint) %>

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket-<%= Rails.env %>

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
